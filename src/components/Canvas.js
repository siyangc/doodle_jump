import React from 'react'

export default function Canvas() {
    return (
        <div style={canvasStyle}>
            
        </div>
    )
}

const canvasStyle = {
    width: "300px",
    height: "600px",
    backgroundColor: "green"
}