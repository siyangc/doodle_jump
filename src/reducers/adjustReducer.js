const initialState = {
    canvasWidth: 600,
    canvasHeight: 450,
    boardWidth: 30,
    boardHeight: 10
}

const adjustReducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state;
    }
}