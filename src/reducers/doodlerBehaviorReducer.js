const initialState = {
    jumpHeight: 100,
    left: 100,
    bottom: 0
}

export const doodlerBehaviorReducer = (state = initialState,action) => {
    switch(action.type){
        default:
            return state;
    }
}